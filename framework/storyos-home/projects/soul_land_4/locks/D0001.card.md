# Lock D0001 — Never treat a receipt whose date precedes the newest active-branch rec

Generated by `storyos learn` on 2026-09-18. Append-only history: this decision
is never deleted, only superseded by a later one that cites it.

```storyos-meta
{
  "card": "learned-lock",
  "id": "D0001",
  "kind": "rejection",
  "status": "active",
  "scope": "story-prose",
  "tokens": [
    "Chapter53 is valid and ready"
  ],
  "enforce_regexes": [
    "Chapter5[23] is valid and ready"
  ]
}
```

**What happened:** Superseded receipts for Chapter52/53 asserted live status; a new agent would have continued rejected work

**Rule now in force:** Never treat a receipt whose date precedes the newest active-branch receipt as live state; a superseded chapter must be marked SUPERSEDED or archived

**Why:** one live-status claim from a dead branch cost a full rebuild cycle

**Regression tokens (hard-grep):** `Chapter53 is valid and ready`

**Machine-enforced patterns:**
- `Chapter5[23] is valid and ready`
